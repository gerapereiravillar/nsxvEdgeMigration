
[project]
name = "edge-migrator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["flask", "typer", "requests"]

[project.scripts]
edge-migrator = "apps.cli.main:app"

[tool.setuptools.packages.find]
where = ["src", "apps"]
